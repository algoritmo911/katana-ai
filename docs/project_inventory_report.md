```markdown
# Katana Project Inventory Report

**Дата составления:** 2025-06-28

**Цель:** Всеобъемлющий аудит проекта Katana, выявление слабых мест, технического долга и подготовка стратегического плана для развития.

## 1. Структура проекта

Проект Katana представляет собой FastAPI приложение с интеграцией Telegram-бота и планировщиком задач APScheduler. Основная логика разделена на несколько ключевых файлов и модулей.

**Древовидная структура (основные компоненты):**

*   `.github/`: Конфигурация GitHub Actions для CI/CD.
    *   `workflows/deploy.yml`: Заглушка для развертывания.
    *   `workflows/main.yml`: CI проверки.
*   `katana/`: Основной код приложения.
    *   `exchange/coinbase_api.py`: Модуль для взаимодействия с API Coinbase (получение цен).
    *   `trader_agent.py`: "Торговый агент", логика принятия решений является заглушкой.
*   `main.py`: Точка входа FastAPI приложения, настройка вебхуков Telegram, APScheduler.
*   `katana_bot.py`: Логика Telegram-бота (обработка команд).
*   `katana_agent.py`: Заглушка для общего "агента".
*   `logging_config.py`: Конфигурация логирования.
*   `requirements.txt`: Зависимости Python.
*   `run_checks.sh`: Скрипт для запуска линтеров, форматтера и тестов.
*   `secrets.toml.example`: Пример файла для секретов.
*   `tests/`: Юнит-тесты.
*   `AGENTS.md`, `README.md`, `CONTRIBUTING.md`: Документация проекта.

**Соответствие архитектуры документации:**
Реальная структура файлов в целом соответствует описаниям в `README.md` и `AGENTS.md`.

## 2. Зависимости (`requirements.txt`)

*   **Состав:** `flake8`, `black`, `pytest`, `pytest-cov`, `coverage`, `requests`, `fastapi`, `uvicorn[standard]`, `python-telegram-bot`, `apscheduler`.
*   **Проблемы:**
    *   🔴 **Версии не зафиксированы:** Критический риск для стабильности и безопасности. `safety` выявил 25 потенциальных уязвимостей из-за этого.
    *   🟡 **Смешаны runtime и dev зависимости:** Рекомендуется разделить на `requirements.txt` и `requirements-dev.txt`.
*   **Рекомендации:**
    *   🔴 Немедленно зафиксировать версии зависимостей (например, `pip freeze > requirements.txt` после установки стабильного набора).
    *   🟠 Разделить зависимости на производственные и для разработки.
    *   🟠 Регулярно проводить аудит безопасности зависимостей с помощью `safety` или `pip-audit` после фиксации версий.
    *   🟢 Рассмотреть переход на инструменты управления зависимостями (Poetry, PDM).

## 3. Скрипты и CLI

*   **`katana_cli.py`**: Не существует.
*   **`n8n`**: Интеграции из репозитория не обнаружено.
*   **`main.py` (FastAPI CLI):**
    *   **Вызов:** `uvicorn main:app --reload --host 0.0.0.0 --port 8000`
    *   **Результат:** Запуск FastAPI сервера с API эндпоинтами (`/health`, `/status`, `/docs` и т.д.).
    *   **Работоспособность:** Работает как стандартное FastAPI приложение.
*   **`run_checks.sh`**:
    *   **Вызов:** `./run_checks.sh`
    *   **Результат (ожидаемый):** Линтинг, форматирование, запуск тестов, отчет о покрытии. Выход с ошибкой при проблемах.
    *   **Результат (фактический):**
        *   Скрипт запускается, но некорректно обрабатывает ошибки (не завершается при ошибках `flake8`, сообщает "All checks passed!" при упавших тестах).
        *   Текущее состояние кода: множество ошибок `flake8`, 4 теста падают.
    *   **Работоспособность:** Требует доработки для корректной обработки ошибок.
*   **Рекомендации:**
    *   🔴 Исправить скрипт `run_checks.sh` (добавить `set -e`, корректно сообщать о результатах).
    *   🔴 Устранить ошибки `flake8` и падающие тесты.

## 4. Тестирование

*   **Перечень тестов:** 20 тестов в `tests/` для `coinbase_api`, `katana_agent`, `katana_bot`, `logging_config`, `trader_agent`.
*   **Статус:** 16 тестов проходят, 4 падают.
    *   `test_logging_config.py::TestLoggingConfig::test_log_directory_and_file_creation`
    *   `test_trader_agent.py::TestTraderAgent::test_get_mock_price`
    *   `test_trader_agent.py::TestTraderAgent::test_log_file_creation_and_content`
    *   `test_trader_agent.py::TestTraderAgent::test_make_decision_logging_calls`
*   **Покрытие по модулям (общее 72%):**
    *   `main.py`: **~0% (критично!)** - отсутствует в отчете.
    *   `katana_bot.py`: **43% (критично!)**
    *   `katana/trader_agent.py`: **54% (критично!)**
    *   `katana/exchange/coinbase_api.py`: **61% (важно)**
    *   `katana_agent.py`: 77%
    *   `logging_config.py`: 79%
*   **Оценка:** Текущее тестовое покрытие недостаточное, особенно для ключевых модулей. Падающие тесты снижают доверие.
*   **Рекомендации:**
    *   🔴 **Исправить все падающие тесты.**
    *   🔴 **Написать тесты для `main.py` (эндпоинты, логика запуска).**
    *   🟠 Значительно увеличить покрытие для `katana_bot.py`, `trader_agent.py`, `coinbase_api.py`.
    *   🟢 Установить целевой процент покрытия (например, >85%) и отслеживать его.

## 5. Безопасность

*   **Линтинг и сканирование (`bandit`):**
    *   Выявлена проблема `B104:hardcoded_bind_all_interfaces` в `main.py` (`host="0.0.0.0"`). Это ожидаемо для веб-сервиса, но требует внимания к конфигурации сети в продакшене.
*   **Уязвимости зависимостей (`safety`):**
    *   🔴 Основной риск – **незакрепленные версии зависимостей**. `safety` не смог провести полный анализ, но предупредил о 25 потенциальных уязвимостях.
*   **Конфиденциальная информация:**
    *   `secrets.toml.example` используется корректно.
    *   `secrets.toml` включен в `.gitignore`.
    *   В `main.py` используются плейсхолдеры для токенов, что безопасно.
*   **Рекомендации:**
    *   🔴 **Закрепить версии зависимостей в `requirements.txt`** и провести повторный анализ `safety`.
    *   🟠 Убедиться, что для продакшена секреты управляются исключительно через переменные окружения.
    *   🟠 Проверить конфигурацию файрвола и сети в продакшене.

## 6. ИИ-интеграции

*   **Текущее состояние:**
    *   `katana_agent.py`: **Заглушка (stub)**, не содержит ИИ-логики.
    *   `katana/trader_agent.py`: Логика принятия решений – **заглушка (stub)**. Получает рыночные данные, но всегда принимает решение "observe".
    *   `self-heal`: Не реализован. Идея в APScheduler (`perform_self_check`) – только комментарий.
    *   `n8n-интеграции`: В коде не обнаружены.
    *   "Интел-модуль": На данный момент отсутствует.
*   **Планируемые нейросети:**
    *   В коде нет свидетельств интеграции или планов по конкретным нейросетям.
    *   Единственный намек – секрет `OPENAI_API_KEY` в `README.md`, что может указывать на планы по использованию OpenAI.
*   **Вывод:** ИИ-функциональность на уровне замысла и заглушек.
*   **Рекомендации:**
    *   🟠 Определить конкретные ИИ-задачи и выбрать технологии/модели.
    *   🟠 Реализовать реальную ИИ-логику в `trader_agent.py` или других модулях.
    *   🟢 Рассмотреть возможность реализации `self-heal` механизмов через APScheduler.

## 7. Синхронизация/облака

*   **`rclone`**: Не используется, конфигураций нет.
*   **`sync_status.json`**: Не используется.
*   **`backup/`, `cloud/` директории**: Отсутствуют.
*   **Локальное сохранение**: `trader_agent.py` сохраняет `trader_data.json` и `decisions.json` в `logs/`. Эти файлы **не игнорируются `.gitignore`** и могут быть закоммичены.
*   **APScheduler (`synchronize_data`)**: Идея синхронизации – только комментарий.
*   **CI/CD (`deploy.yml`)**:
    *   Развертывание в облако – **заглушка (placeholder)**.
    *   Уведомления Telegram о деплое – **заглушка (placeholder)**.
    *   Использование секретов `RAILWAY_TOKEN`, `SSH_PRIVATE_KEY` указывает на планы по деплою.
*   **Вывод:** Системы синхронизации, бэкапа и облачного развертывания не реализованы.
*   **Рекомендации:**
    *   🟠 Добавить `logs/*.json` или `logs/` в `.gitignore`, если эти файлы не предназначены для версионирования.
    *   🟠 Реализовать шаг развертывания в `deploy.yml`, если планируется автоматический деплой.
    *   🟢 Рассмотреть и реализовать механизмы синхронизации/бэкапа данных при необходимости.

## 8. Логи и наблюдаемость

*   **Текущая система:**
    *   `logging_config.py`: Настраивает логгер `katana` для вывода в консоль и `logs/app.log`.
    *   `trader_agent.py`: Имеет собственную конфигурацию логгеров (`KatanaTrader`, `KatanaTraderData`) для файлов `logs/trader.log`, `logs/trader_data.log`.
    *   🔴 **Проблема:** Файлы `.log` (`app.log`, `trader.log`, `trader_data.log`) **не создаются `FileHandler`** в текущем окружении, несмотря на попытки их конфигурации. Причина неясна, требует отладки. Прямая запись файлов в `logs/` работает.
    *   **Файлы `katana_events.log`, `handler_stdout.log`, `stderr.log`**: Не найдены и не управляются приложением.
*   **Использование:** Логи (если бы писались в файлы) содержали бы информацию для дебага и мониторинга. JSON-файлы `decisions.json` и `trader_data.json` служат для сохранения состояния/истории.
*   **Проблемы:**
    *   🔴 Неработоспособность файлового логирования.
    *   🟡 Децентрализованная конфигурация логгеров.
    *   🟡 Текстовый формат логов (менее удобен для анализа, чем JSON).
    *   🟡 Отсутствие ротации логов.
*   **Рекомендации по архитектуре логов:**
    *   🔴 **Исправить проблему с созданием `.log` файлов.**
    *   🟠 **Централизовать конфигурацию логирования** (например, через `logging.dictConfig`).
    *   🟠 **Внедрить структурированное логирование (JSON)** для упрощения анализа и интеграции с системами мониторинга (Elasticsearch, Loki).
    *   🟠 **Добавить ротацию логов** (`RotatingFileHandler` или `TimedRotatingFileHandler`).
    *   🟢 Рассмотреть использование ID трассировки для корреляции событий.

## 9. Пошаговый план апгрейда (Roadmap)

### Фаза 1: Стабилизация и Базовая Безопасность (Краткосрочно)

1.  🔴 **Зависимости:**
    *   Зафиксировать версии всех зависимостей в `requirements.txt`.
    *   Провести аудит `safety` после фиксации. Устранить критические уязвимости.
2.  🔴 **Тестирование:**
    *   Исправить все 4 падающих теста.
    *   Начать написание тестов для `main.py` (базовые эндпоинты).
3.  🔴 **Скрипты:**
    *   Исправить `run_checks.sh` для корректной обработки ошибок и отчетности.
    *   Устранить все ошибки `flake8`.
4.  🔴 **Логирование:**
    *   **Тщательно расследовать и исправить проблему с несозданием файлов `.log` через `FileHandler`.** Это приоритет для отладки и мониторинга.
5.  🟠 **Конфигурация:**
    *   Добавить `logs/*.json` в `.gitignore`.

### Фаза 2: Улучшение Качества и CI/CD (Среднесрочно)

1.  🟠 **Тестирование:**
    *   Достичь >80% покрытия для `main.py`, `katana_bot.py`, `trader_agent.py`.
2.  🟠 **Логирование:**
    *   Централизовать конфигурацию логирования.
    *   Внедрить структурированное логирование (JSON).
    *   Добавить ротацию логов.
3.  🟠 **Зависимости:**
    *   Разделить на `requirements.txt` и `requirements-dev.txt`.
4.  🟠 **CI/CD:**
    *   Реализовать рабочий шаг развертывания в `deploy.yml` для выбранной платформы (Railway/VPS).
    *   Реализовать уведомления о статусе деплоя.
5.  🟢 **Безопасность:**
    *   Убедиться, что секреты в продакшене управляются строго через переменные окружения.

### Фаза 3: Развитие Функциональности (Долгосрочно)

1.  🟠 **ИИ-интеграции:**
    *   Определить MVP для ИИ-логики в `trader_agent.py`.
    *   Начать реализацию (например, интеграция с OpenAI API, если это выбранное направление).
    *   Развивать `katana_agent.py` от заглушки к полезному компоненту.
2.  🟢 **Синхронизация/Бэкап:**
    *   Реализовать механизмы синхронизации или бэкапа данных, если это необходимо.
3.  🟢 **Self-Heal / Мониторинг:**
    *   Реализовать функции самодиагностики и восстановления (self-heal) через APScheduler.
    *   Интегрировать систему логирования с платформой мониторинга (ELK, Loki).
4.  🟢 **Документация:**
    *   Поддерживать документацию (`README.md`, `AGENTS.md`, комментарии в коде) в актуальном состоянии.

## 10. Приоритетные действия

*   🔴 **Исправить проблему с созданием файлов логов (`.log`).**
*   🔴 **Зафиксировать версии зависимостей в `requirements.txt` и провести аудит `safety`.**
*   🔴 **Исправить падающие тесты (4 шт.).**
*   🔴 **Исправить скрипт `run_checks.sh`.**
*   🔴 **Устранить ошибки `flake8`.**
*   🔴 **Начать писать тесты для `main.py`.**
*   🟠 Добавить `logs/*.json` в `.gitignore`.
*   🟠 Увеличить тестовое покрытие для критических модулей.
*   🟠 Реализовать рабочий CI/CD пайплайн развертывания.
*   🟠 Внедрить структурированное логирование (JSON) и ротацию логов.
*   🟠 Разделить зависимости (runtime/dev).
*   🟠 Начать разработку реальной ИИ-логики для агентов.

## 11. Рекомендации по архитектуре/CI

*   **Архитектура:**
    *   Рассмотреть внедрение более строгой конфигурации через Pydantic Settings для управления переменными окружения.
    *   Усилить модульность, особенно если ИИ-компоненты будут разрастаться.
    *   Для `TraderAgent` и других потенциально долгоживущих или ресурсоемких задач рассмотреть использование фоновых задач (уже есть APScheduler, но можно расширить на Celery/Redis, если масштабы потребуют).
*   **CI/CD:**
    *   Обеспечить, чтобы `run_checks.sh` был надежной частью CI пайплайна.
    *   Добавить этап автоматического аудита безопасности зависимостей в CI.
    *   Рассмотреть возможность автоматического создания тегов и релизов при деплое на продакшен.
    *   Настроить уведомления о результатах CI/CD (уже есть заглушка).

Этот отчет должен служить основой для дальнейшего планирования и улучшения проекта Katana.
```
