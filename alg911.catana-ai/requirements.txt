Flask>=2.0
Flask-SocketIO>=5.0
python-socketio>=5.0
requests>=2.20
pydantic>=2.0
# For testing & linting
pytest>=7.0
flake8>=5.0
black>=22.0
# Note: Eventlet or Gevent would be needed for production SocketIO async_mode,
# but 'threading' (default for Flask-SocketIO if others not found) is used for simplicity in current backend.
# If specific versions are required by the environment, pin them.
# Example: Flask==2.3.2
