# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtualenv
.env
.venv
env/
venv/
ENV/
VENV/
pip-freeze.txt
pip-selfcheck.json

# IDEs and editors
.idea/
.vscode/
*.swp
*~
*.sublime-project
*.sublime-workspace

# Node.js / UI (relative to this .gitignore file in katana/)
ui/node_modules/
ui/.vite/
ui/dist/
ui/.DS_Store
ui/npm-debug.log*
ui/yarn-debug.log*

# Old UI project paths (if they were at the root, relative to /app)
# These might need to be in a .gitignore at /app level if those dirs still exist there.
# For now, this .gitignore is inside /app/katana/.
# If katana-secrets-ui is at /app/katana-secrets-ui, this won't affect it.
# ../katana-secrets-ui/node_modules/ # Example if it was a sibling and we wanted to ignore from here

# Katana specific (paths relative to this .gitignore file in katana/)
# Secrets and credentials
data/secrets_temp.json
data/*.bak
services/secrets_harvester/secrets_temp.json
services/secrets_harvester/*.bak
services/api_integrations/token.json
services/api_integrations/credentials.json # User should provide this, but ignore if accidentally committed

# General .env files at various locations within katana/
*.env
.env.*.local
.env.local
*/*.env
*/*/*.env

# Logs
*.log
logs/ # Ignore top-level logs/ dir in katana/
mci_agent/logs/
services/api_integrations/logs/

# OS generated files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# PyiCloud session data
# If pyicloud saves session in its execution dir (e.g. katana/services/secrets_harvester/):
services/secrets_harvester/.pyicloud/

# Other potentially sensitive or large files
# data/large_models/

# Coverage reports
.coverage
coverage.xml
htmlcov/
.pytest_cache/

# Build artifacts from other languages if used
*.class
*.jar
*.war

# Temporary files
*.tmp
*.temp
temp/
tmp/

# Specific temporary files from tools or scripts (e.g. test outputs)
services/secrets_harvester/tests/temp_test_secrets_output_for_save/
services/secrets_harvester/tests/temp_test_icloud_save_output/
