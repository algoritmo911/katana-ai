{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Katana Command Log",
  "description": "Schema for Katana command logs, designed for distributed tracing and analytics.",
  "type": "object",
  "properties": {
    "trace_id": {
      "description": "Unique identifier for a trace, spanning multiple commands.",
      "type": "string",
      "format": "uuid"
    },
    "span_id": {
      "description": "Unique identifier for a single command execution (a span in the trace).",
      "type": "string"
    },
    "parent_id": {
      "description": "The span_id of the parent command, if any.",
      "type": ["string", "null"]
    },
    "timestamp": {
      "description": "Timestamp of the event in ISO 8601 format.",
      "type": "string",
      "format": "date-time"
    },
    "command": {
      "description": "The name of the command being executed.",
      "type": "string"
    },
    "user_id": {
      "description": "The ID of the user who initiated the command.",
      "type": ["string", "null"]
    },
    "input": {
      "description": "The input arguments and keyword arguments for the command.",
      "type": "object",
      "properties": {
        "args": { "type": "array" },
        "kwargs": { "type": "object" }
      }
    },
    "output": {
      "description": "The result of the command execution.",
      "type": "object"
    },
    "error": {
      "description": "Any error that occurred during execution.",
      "type": ["object", "null"],
      "properties": {
        "type": { "type": "string" },
        "message": { "type": "string" },
        "stacktrace": { "type": "string" }
      }
    },
    "duration_ms": {
      "description": "The duration of the command execution in milliseconds.",
      "type": "number"
    },
    "metadata": {
      "description": "Any additional metadata about the command or its environment.",
      "type": "object"
    }
  },
  "required": ["trace_id", "span_id", "timestamp", "command", "duration_ms"]
}
