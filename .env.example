KATANA_TELEGRAM_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"
ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY"
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# --- Logging Configuration ---
# LOG_LEVEL: Set the desired logging level (e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Defaults to INFO if not set.
LOG_LEVEL="INFO"

# LOG_FILE_PATH: Absolute path to the log file. If commented out or empty, logs will only go to console.
# Example: /var/log/katana-bot/katana-bot.log
# Ensure the directory exists and the bot process has write permissions.
# LOG_FILE_PATH="/var/log/katana-bot/katana-bot.log"

# --- Heartbeat Configuration ---
# HEARTBEAT_FILE_PATH: Absolute path for the heartbeat file.
# If commented out or empty, heartbeat mechanism will be disabled.
# Example: /tmp/katana_bot_heartbeat.txt or /var/run/katana-bot/heartbeat.txt
# Ensure the directory exists and the bot process has write permissions.
# HEARTBEAT_FILE_PATH="/tmp/katana_bot_heartbeat.txt"

# HEARTBEAT_INTERVAL_SECONDS: How often the bot updates the heartbeat file.
# Defaults to 30 seconds if not set.
HEARTBEAT_INTERVAL_SECONDS="30"

# HEARTBEAT_MAX_AGE_SECONDS: Used by check_heartbeat.py script.
# How old the heartbeat file can be before check_heartbeat.py considers it stale.
# Should be greater than HEARTBEAT_INTERVAL_SECONDS (e.g., 2-3 times).
# Defaults to 120 seconds if not set for check_heartbeat.py.
# HEARTBEAT_MAX_AGE_SECONDS="120"

# --- Alerting Configuration (Conceptual - for check_heartbeat.py) ---
# These are used by the conceptual `send_telegram_alert` function in `tools/check_heartbeat.py`.
# You would need to implement actual alert sending logic.
# ALERT_TELEGRAM_BOT_TOKEN="YOUR_ALERT_BOT_TOKEN"
# ALERT_TELEGRAM_CHAT_ID="YOUR_TELEGRAM_CHAT_ID_FOR_ALERTS"

# --- Self-Healing Orchestrator Configuration ---
# SELF_HEAL_ENABLED: Set to "true" to enable the self-healing orchestrator.
# Defaults to "false" if not set.
SELF_HEAL_ENABLED="false"

# SELF_HEAL_LOG_PATH: Absolute path to the log file the orchestrator should monitor.
# Defaults to LOG_FILE_PATH if not set.
# SELF_HEAL_LOG_PATH="/var/log/katana-bot/katana-bot.log"

# SELF_HEAL_SERVICE_NAME: The name of the systemd service to restart (e.g., "katana-bot.service").
# This is REQUIRED if self-healing is enabled.
SELF_HEAL_SERVICE_NAME="katana-bot.service"

# SELF_HEAL_INTERVAL_SECONDS: How often the orchestrator runs its health checks.
# Defaults to 60 seconds.
SELF_HEAL_INTERVAL_SECONDS="60"

# SELF_HEAL_ERROR_THRESHOLD: The number of error lines in the log file to trigger a restart.
# Defaults to 10.
SELF_HEAL_ERROR_THRESHOLD="10"

# SELF_HEAL_NOTIFICATION_CHAT_ID: The Telegram Chat ID to send alert notifications to.
# This is REQUIRED if self-healing is enabled.
SELF_HEAL_NOTIFICATION_CHAT_ID="YOUR_TELEGRAM_CHAT_ID_FOR_ALERTS"
